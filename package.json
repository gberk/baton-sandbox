{
  "name": "baton-sandbox",
  "version": "1.0.0",
  "description": "Sandbox project for testing the Baton pipeline",
  "type": "module",
  "bin": { "greet": "./dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/cli.ts"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
